name: CI/CD

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

permissions:
  contents: read
  pull-requests: write
  packages: write

jobs:
  pipeline:
    uses: hugobrun343/template-spring-micro-service-CI/.github/workflows/spring-boot-pipeline.yml@v1.0.1
    with:
      cd-branches: '["main"]'
    secrets:
      github-token: ${{ secrets.GITHUB_TOKEN }}