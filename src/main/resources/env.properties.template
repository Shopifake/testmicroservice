# Local development environment variables
# Copy this file to .env.properties in the same directory for local development

# Application
APP_NAME=microservice-template
PORT=8080

# Database - H2 (dev/test)
DB_URL=jdbc:h2:mem:testdb
DB_DRIVER=org.h2.Driver
DB_DIALECT=org.hibernate.dialect.H2Dialect
DB_USERNAME=sa
DB_PASSWORD=

# Database - PostgreSQL (prod/staging)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=microservice_dev