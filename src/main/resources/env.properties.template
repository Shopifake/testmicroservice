# Environment variables template for local development
# Copy this file and rename it to .env.properties.local for your local configuration

# Application configuration
APP_NAME=microservice-template
PORT=8080

# Service configuration
EVENTS_SERVICE_URI=http://localhost:8091

# Database configuration (if needed)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=microservice_dev
DB_USERNAME=postgres
DB_PASSWORD=password

# External services (if needed)
# API_KEY=your-api-key
# SERVICE_URL=http://localhost:8081